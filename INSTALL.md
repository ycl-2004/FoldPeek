# FoldPeek 安装说明

在 Finder 里选中一个**文件夹**，按**空格**，直接看到里面有什么——不用点进去。

---

## 系统要求

- macOS 13 或更高
- 无需安装任何依赖，不联网

## 一、装进「应用程序」

1. 解压 `FoldPeek.zip`
2. 把 `FoldPeek.app` 拖进**「应用程序」**文件夹

> **装好后别再挪动它。** 这个 App 是用本地签名打包的，换位置后系统对扩展的注册会失效，得重新走一遍第三步。

## 二、解除下载隔离（必须做，否则打不开）

双击会看到：

> **「FoldPeek」已损坏，无法打开。你应该将它移到废纸篓。**

**文件没有损坏。** 这是 macOS 对「从网上下载 + 没有付费开发者证书签名」的 App 的固定报错，措辞很吓人但和文件完整性无关。

打开**「终端」**（聚焦搜索 `Terminal`），粘贴这一行并回车：

```bash
xattr -dr com.apple.quarantine /Applications/FoldPeek.app
```

没有任何输出就是成功了。

<details>
<summary>这一行到底做了什么？</summary>

macOS 会给所有下载来的文件打一个叫 `com.apple.quarantine` 的标记。这条命令**只是移除这个标记**，不改动 App 本身，也不关闭系统里任何其他安全机制。

但要说清楚：**这一步的本质是「我信任发我这个文件的人」。** 系统本来想帮你拦一道，你手动放行了。所以——

- 只对**认识的人当面/私下发给你的文件**这么做
- 不要对从网上随便下载的东西这么做
- 如果你想先自己确认一下，往下看「怎么自己验证」

</details>

## 三、在系统设置里启用

1. **系统设置 → 通用 → 登录项与扩展**
2. 找到 **「快速查看」**，点右边的 ⓘ
3. 把 **FoldPeek** 的开关打开

这一步没法自动完成——这个 App 刻意不包含任何会自动修改系统状态的脚本。

## 四、开始用

Finder 里选中任意文件夹 → 按**空格**。

| 操作 | 效果 |
|---|---|
| 点文件夹那一行 | 展开 / 收起（整行都可以点，不用瞄小三角） |
| 点文件 | 右侧显示内容 |
| 搜索框 | 过滤当前已展开的条目 |
| 拖中间分隔线 | 调整左右比例 |

代码文件有语法高亮、括号配对着色、缩进引导线和行号；Markdown 会渲染成排版好的文档。

---

## 怎么自己验证（可选，但推荐）

不用只凭信任，这些都能自己查：

**看它要了什么权限**——这是最能说明问题的一项：

```bash
codesign -d --entitlements - /Applications/FoldPeek.app
codesign -d --entitlements - /Applications/FoldPeek.app/Contents/PlugIns/FoldPeekPreviewExtension.appex
```

应该只看到：

```
宿主 App:  com.apple.security.app-sandbox                        （1 项）
扩展:      com.apple.security.app-sandbox
          com.apple.security.files.user-selected.read-only      （2 项）
```

**两个都在沙盒里，扩展只有「读你选中的文件」这一项权限**，没有网络、没有写入、没有其他任何东西。

**核对文件没被中途篡改**（跟发你的人给的 SHA-256 对一下）：

```bash
shasum -a 256 FoldPeek.zip
```

**读源码**——整个项目目前有 14 个 Swift 源文件，约 3300 行。`docs/SECURITY_AUDIT.md` 记录了当前安全边界、验证方法和残留风险。

---

## 它不会做的事

- 不联网，没有网络权限
- 不安装后台服务、不注册开机启动项、不监听任何端口
- 不修改、删除或移动你的任何文件（扩展只有只读权限）
- 不跟随符号链接
- 不打开外部程序，不收集任何数据

## 已知限制

- **方向键不归它管。** 预览打开时方向键由 Finder 控制（用来切换文件），这是 Quick Look 的设计，第三方扩展抢不到键盘。
- **搜索只搜已展开的层级。** 没展开过的文件夹里的内容搜不到——这是刻意的，否则一次按键就可能触发整棵目录树的磁盘读取。
- 单个文件夹最多列 2000 项，展开最多 8 层，文本预览最多 256 KB。都是防止超大目录拖垮预览的硬上限。

## 卸载

1. 系统设置里把「快速查看」下的 FoldPeek 开关关掉
2. 把 `/Applications/FoldPeek.app` 拖到废纸篓

没有残留文件——它不往系统任何地方写东西。

---

## 出问题了？

**按空格还是系统默认的文件夹预览**
扩展没启用，回到第三步。如果开关是开的，试试关掉再打开。

**装完没反应 / 显示旧版本**
终端跑一下，再重新按空格：
```bash
pkill -f FoldPeekPreviewExtension
```

**挪动过 App 之后失效了**
挪回「应用程序」，或者重新走第三步。
